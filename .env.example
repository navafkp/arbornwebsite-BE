# Copy to .env (git-ignored) and fill in real values for local dev.

SECRET_KEY=HakRnY0MIQHq4p2GLGzuKd4LEkGRrBoOThD1vmfyYqz4KrAaKtlyDvv92KINilYkcro
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Postgres
DB_NAME=arborn
DB_USER=arborn
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# The Next.js frontend origins allowed to call this API
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://navafkp.github.io

# Same value as the frontend's NEXT_PUBLIC_GOOGLE_CLIENT_ID — used to verify
# incoming Google ID tokens were issued for this app.
GOOGLE_CLIENT_ID=

# Email (for OTP codes). Dev default just prints to the console — leave
# these blank until you're ready to send real emails.
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Arborn <no-reply@arborn.com>
